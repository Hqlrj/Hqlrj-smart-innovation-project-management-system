<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.springbootproject.mapper.NoticeMapper">

    <resultMap id="noticeResultMap" type="org.example.springbootproject.pojo.Notice">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="publisherId" column="publisher_id"/>
        <result property="publisherName" column="publisher_name"/>
        <result property="publisherRole" column="publisher_role"/>
        <result property="isTop" column="is_top"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <!-- 公告列表 -->
    <select id="list" resultMap="noticeResultMap">
        SELECT id, title, content, publisher_id, publisher_name, publisher_role,
               is_top, status, create_time, update_time
        FROM notice
        <where>
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            AND status = 'normal'
        </where>
        ORDER BY is_top DESC, create_time DESC
    </select>

    <!-- 根据ID查询公告 -->
    <select id="getById" resultMap="noticeResultMap">
        SELECT id, title, content, publisher_id, publisher_name, publisher_role,
               is_top, status, create_time, update_time
        FROM notice
        WHERE id = #{id}
    </select>

    <!-- 新增公告 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notice (title, content, publisher_id, publisher_name, publisher_role,
                            is_top, status, create_time)
        VALUES (#{title}, #{content}, #{publisherId}, #{publisherName}, #{publisherRole},
                #{isTop}, #{status}, #{createTime})
    </insert>

    <!-- 删除公告 -->
    <delete id="delete">
        DELETE FROM notice WHERE id = #{id}
    </delete>

</mapper>
